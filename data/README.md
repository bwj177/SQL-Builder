# Local Runtime Data

This directory is used by the local server to store runtime data:

- `models.json`
- `queries.json`
- `llm_config.json`

These files may contain private DDL, table names, saved query templates, local model paths, or AI configuration. They are intentionally ignored by Git.

